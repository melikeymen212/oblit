{
    "en": "h1(Server API: Complete Reference)\nh3(Everything you need to know about building OBLIT servers.)\n\nThe <code>Server</code> class is the heart of any OBLIT application. It provides an event-driven, high-performance TCP server with automatic stream reassembly and crash-free error handling.\n\n![info](All Server instances extend Node.js's EventEmitter, so you can use familiar event patterns. The API is intentionally minimal but powerful.)!++\n\n!(line)\n\nh2(📦 Constructor)\n\nh3(<code>new Server(options)</code>)\n\nCreates a new server instance. The server does **not** start listening until you call <code>st()</code>.\n\n<div class=\"table-wrapper\"><table><thead><tr><th>Parameter</th><th>Type</th><th>Default</th><th>Required</th><th>Description</th></tr></thead><tbody><tr><td><code>options</code></td><td><code>Object</code></td><td><code>{}</code></td><td>No</td><td>Configuration object for the server</td></tr><tr><td><code>options.port</code></td><td><code>Number</code></td><td><code>3000</code></td><td>No</td><td>The TCP port number to bind to (0-65535)</td></tr><tr><td><code>options.silent</code></td><td><code>Boolean</code></td><td><code>false</code></td><td>No</code></td><td>If <code>true</code>, suppresses all internal error logging</td></tr></tbody></table></div>\n\n<h3(Usage Examples)\n\n<div style=\"display:grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap:20px; margin:25px 0;\">\n\n<div style=\"background:var(--box-info-bg); padding:20px; border-radius:8px; border-left:4px solid var(--primary);\">\n<strong style=\"color:var(--primary);\">📌 Default Port (3000)</strong><br><br>\n<code style=\"background:transparent;\">const server = new Server();<br>// Listens on port 3000</code>\n</div>\n\n<div style=\"background:var(--box-tip-bg); padding:20px; border-radius:8px; border-left:4px solid #22c55e;\">\n<strong style=\"color:#22c55e;\">📌 Custom Port</strong><br><br>\n<code style=\"background:transparent;\">const server = new Server({ port: 8080 });<br>// Listens on port 8080</code>\n</div>\n\n<div style=\"background:var(--box-warn-bg); padding:20px; border-radius:8px; border-left:4px solid #f97316;\">\n<strong style=\"color:#f97316;\">📌 Silent Mode</strong><br><br>\n<code style=\"background:transparent;\">const server = new Server({<br>  port: 9000,<br>  silent: true<br>});<br>// No error logs</code>\n</div>\n\n</div>\n\n!(line)\n\nh2(⚙️ Methods)\n\nh3(<code>server.st([callback])</code>)\n\nStarts the TCP listener and begins accepting connections. This is an alias for <code>start()</code>—shorter for convenience.\n\n<div class=\"table-wrapper\"><table><thead><tr><th>Parameter</th><th>Type</th><th>Required</th><th>Description</th></tr></thead><tbody><tr><td><code>callback</code></td><td><code>Function</code></td><td>No</td><td>Called once when the server successfully starts listening. Receives no arguments.</td></tr></tbody></table></div>\n\n<h3(Examples)\n\n![code](const { Server } = require('oblit');\n\nconst app = new Server({ port: 8080 });\n\n// Start with callback\napp.st(() => {\n    console.log('✅ Server started on port 8080');\n    console.log('Ready to accept connections!');\n});\n\n// Or start without callback\napp.st();)!++//\n\n![tip](The callback is useful for initialization tasks like loading game state, connecting to databases, or sending \"server ready\" notifications.)!++\n\nh3(<code>server.bc(message)</code>)\n\nBroadcasts a message to **all** currently connected clients. This is an alias for <code>broadcast()</code>.\n\n<div class=\"table-wrapper\"><table><thead><tr><th>Parameter</th><th>Type</th><th>Required</th><th>Description</th></tr></thead><tbody><tr><td><code>message</code></td><td><code>String | Object | Buffer</code></td><td>Yes</td><td>The data to broadcast. Type is automatically detected (TXT/JSON/RAW).</td></tr></tbody></table></div>\n\n<h3(Performance Details)\n\n<div style=\"background:var(--box-tip-bg); padding:20px; border-radius:8px; margin:20px 0; border-left:4px solid #22c55e;\">\n<strong style=\"color:#22c55e;\">⚡ Optimization:</strong><br><br>\nOBLIT packs the data **once** and writes the same buffer reference to all connected sockets. This means:\n<ul style=\"margin:10px 0 0 0;\">\n<li>✅ Zero memory overhead per client</li>\n<li>✅ Minimal CPU usage</li>\n<li>✅ Fastest possible broadcast</li>\n</ul>\n</div>\n\n<h3(Examples)\n\n![code](const app = new Server({ port: 8080 });\n\napp.on('data', (message, socket) => {\n    // Broadcast text message\n    app.bc(`User said: ${message}`);\n    \n    // Broadcast JSON object\n    app.bc({ type: 'chat', user: socket.remoteAddress, text: message });\n    \n    // Broadcast binary data\n    const gameState = Buffer.from([1, 2, 3, 4]);\n    app.bc(gameState);\n});\n\napp.st();)!++//\n\n![info](Broadcast is one-way only. Clients won't receive responses to their own broadcasts. If you need bidirectional communication, use the <code>socket</code> parameter in the <code>'data'</code> event.)!++\n\n!(line)\n\nh2(📡 Events)\n\nThe Server class extends EventEmitter, so you can use all standard EventEmitter methods (<code>on</code>, <code>once</code>, <code>off</code>, etc.).\n\nh3(<code>'data'</code> Event)\n\nEmitted whenever a complete packet is received from a client. This is the primary event for handling incoming messages.\n\n<div class=\"table-wrapper\"><table><thead><tr><th>Event Name</th><th>Callback Arguments</th><th>When Emitted</th></tr></thead><tbody><tr><td><code>'data'</code></td><td><code>(message, socket)</code></td><td>When a complete packet arrives from any client</td></tr></tbody></table></div>\n\n<h3(Callback Parameters)\n\n<div class=\"table-wrapper\"><table><thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>message</code></td><td><code>String | Object | Buffer</code></td><td>The unpacked payload. Type depends on the OpCode sent by client.</td></tr><tr><td><code>socket</code></td><td><code>net.Socket</code></td><td>The Node.js socket object of the client that sent the message. Use this to send replies or identify clients.</td></tr></tbody></table></div>\n\n<h3(Examples)\n\n![code](const { Server, OP } = require('oblit');\nconst core = require('oblit/src/c');\n\nconst app = new Server({ port: 8080 });\n\n// Handle text messages\napp.on('data', (message, socket) => {\n    console.log(`Received from ${socket.remoteAddress}:`, message);\n    \n    // Send reply to the sender only\n    if (message === 'PING') {\n        const pongPacket = core.p(OP.TXT, 'PONG');\n        socket.write(pongPacket);\n    }\n    \n    // Broadcast to everyone else\n    if (typeof message === 'string' && message.startsWith('!')) {\n        app.bc(`Announcement: ${message.substring(1)}`);\n    }\n});\n\n// Handle JSON objects\napp.on('data', (message, socket) => {\n    if (typeof message === 'object' && !Buffer.isBuffer(message)) {\n        if (message.type === 'login') {\n            console.log(`User ${message.username} logged in`);\n            socket.playerId = message.username;\n        }\n    }\n});\n\n// Handle binary data\napp.on('data', (message, socket) => {\n    if (Buffer.isBuffer(message)) {\n        // Process game coordinates, sensor data, etc.\n        const playerId = message.readUInt16BE(0);\n        const x = message.readUInt16BE(2);\n        const y = message.readUInt16BE(4);\n        console.log(`Player ${playerId} moved to (${x}, ${y})`);\n    }\n});\n\napp.st();)!++//\n\nh3(<code>'connect'</code> Event)\n\nEmitted when a new client successfully connects to the server.\n\n<div class=\"table-wrapper\"><table><thead><tr><th>Event Name</th><th>Callback Arguments</th><th>When Emitted</th></tr></thead><tbody><tr><td><code>'connect'</code></td><td><code>(socket)</code></td><td>Immediately after a new client establishes a TCP connection</td></tr></tbody></table></div>\n\n<h3(Examples)\n\n![code](const app = new Server({ port: 8080 });\n\napp.on('connect', (socket) => {\n    const clientId = `${socket.remoteAddress}:${socket.remotePort}`;\n    console.log(`✅ New connection: ${clientId}`);\n    \n    // Store custom data on socket\n    socket.connectedAt = Date.now();\n    socket.playerId = null;\n    \n    // Send welcome message\n    const welcome = { type: 'welcome', message: 'Connected to server!' };\n    app.bc(welcome);\n});\n\napp.st();)!++//\n\nh3(<code>'disconnect'</code> Event)\n\nEmitted when a client disconnects (gracefully or unexpectedly).\n\n<div class=\"table-wrapper\"><table><thead><tr><th>Event Name</th><th>Callback Arguments</th><th>When Emitted</th></tr></thead><tbody><tr><td><code>'disconnect'</code></td><td><code>(socket)</code></td><td>When a client's TCP connection is closed</td></tr></tbody></table></div>\n\n<h3(Examples)\n\n![code](const app = new Server({ port: 8080 });\n\napp.on('disconnect', (socket) => {\n    const clientId = socket.playerId || `${socket.remoteAddress}:${socket.remotePort}`;\n    console.log(`❌ Client disconnected: ${clientId}`);\n    \n    // Cleanup\n    if (socket.playerId) {\n        delete gameState.players[socket.playerId];\n    }\n    \n    // Notify other clients\n    app.bc({ type: 'player_left', playerId: socket.playerId });\n});\n\napp.st();)!++//\n\nh3(<code>'error'</code> Event)\n\nEmitted when a server-level error occurs (e.g., port already in use).\n\n<div class=\"table-wrapper\"><table><thead><tr><th>Event Name</th><th>Callback Arguments</th><th>When Emitted</th></tr></thead><tbody><tr><td><code>'error'</code></td><td><code>(error)</code></td><td>When a server-level error occurs</td></tr></tbody></table></div>\n\n<h3(Examples)\n\n![code](const app = new Server({ port: 8080 });\n\napp.on('error', (error) => {\n    console.error('❌ Server error:', error.message);\n    \n    if (error.code === 'EADDRINUSE') {\n        console.log('Port 8080 is already in use. Trying port 8081...');\n        app.st(); // This won't work, but shows the pattern\n    }\n});\n\napp.st();)!++//\n\n!(line)\n\nh2(💡 Best Practices)\n\n<div style=\"display:grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap:15px; margin:25px 0;\">\n\n<div style=\"background:var(--box-tip-bg); padding:20px; border-radius:8px; border-left:4px solid #22c55e;\">\n<strong style=\"color:#22c55e;\">✅ Store Client Data</strong><br><br>\nAttach custom properties to the <code>socket</code> object to track client state:\n<code style=\"background:transparent;\">socket.playerId = 'user123';</code>\n</div>\n\n<div style=\"background:var(--box-warn-bg); padding:20px; border-radius:8px; border-left:4px solid #f97316;\">\n<strong style=\"color:#f97316;\">⚠️ Handle Errors</strong><br><br>\nAlways listen for the <code>'error'</code> event to handle server-level issues gracefully.\n</div>\n\n<div style=\"background:var(--box-info-bg); padding:20px; border-radius:8px; border-left:4px solid var(--primary);\">\n<strong style=\"color:var(--primary);\">📦 Use Binary for Games</strong><br><br>\nFor high-frequency data (like player positions), use Buffer/RAW mode for maximum performance.\n</div>\n\n</div>\n\n![tip](Want to see a complete working example? Check out the **[Chat Tutorial](#!/tut_chat)** or **[Game Server Tutorial](#!/tut_game)** for real-world implementations.)!++",
    "tr": "h1(Sunucu API: Tam Referans)\nh3(OBLIT sunucuları oluşturma hakkında bilmeniz gereken her şey.)\n\n<code>Server</code> sınıfı, herhangi bir OBLIT uygulamasının kalbidir. Otomatik akış yeniden birleştirme ve çökmesiz hata yönetimi ile olay odaklı, yüksek performanslı bir TCP sunucusu sağlar.\n\n![info](Tüm Server örnekleri Node.js'in EventEmitter'ını genişletir, bu yüzden tanıdık olay desenlerini kullanabilirsiniz. API kasıtlı olarak minimal ama güçlüdür.)!++\n\n!(line)\n\nh2(📦 Constructor)\n\nh3(<code>new Server(options)</code>)\n\nYeni bir sunucu örneği oluşturur. Sunucu <code>st()</code> çağrılana kadar dinlemeye **başlamaz**.\n\n<div class=\"table-wrapper\"><table><thead><tr><th>Parametre</th><th>Tip</th><th>Varsayılan</th><th>Gerekli</th><th>Açıklama</th></tr></thead><tbody><tr><td><code>options</code></td><td><code>Object</code></td><td><code>{}</code></td><td>Hayır</td><td>Sunucu için yapılandırma nesnesi</td></tr><tr><td><code>options.port</code></td><td><code>Number</code></td><td><code>3000</code></td><td>Hayır</td><td>Bağlanılacak TCP port numarası (0-65535)</td></tr><tr><td><code>options.silent</code></td><td><code>Boolean</code></td><td><code>false</code></td><td>Hayır</code></td><td><code>true</code> ise, tüm dahili hata günlüğünü bastırır</td></tr></tbody></table></div>\n\n<h3(Kullanım Örnekleri)\n\n<div style=\"display:grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap:20px; margin:25px 0;\">\n\n<div style=\"background:var(--box-info-bg); padding:20px; border-radius:8px; border-left:4px solid var(--primary);\">\n<strong style=\"color:var(--primary);\">📌 Varsayılan Port (3000)</strong><br><br>\n<code style=\"background:transparent;\">const server = new Server();<br>// 3000 portunda dinler</code>\n</div>\n\n<div style=\"background:var(--box-tip-bg); padding:20px; border-radius:8px; border-left:4px solid #22c55e;\">\n<strong style=\"color:#22c55e;\">📌 Özel Port</strong><br><br>\n<code style=\"background:transparent;\">const server = new Server({ port: 8080 });<br>// 8080 portunda dinler</code>\n</div>\n\n<div style=\"background:var(--box-warn-bg); padding:20px; border-radius:8px; border-left:4px solid #f97316;\">\n<strong style=\"color:#f97316;\">📌 Sessiz Mod</strong><br><br>\n<code style=\"background:transparent;\">const server = new Server({<br>  port: 9000,<br>  silent: true<br>});<br>// Hata günlüğü yok</code>\n</div>\n\n</div>\n\n!(line)\n\nh2(⚙️ Metodlar)\n\nh3(<code>server.st([callback])</code>)\n\nTCP dinleyicisini başlatır ve bağlantıları kabul etmeye başlar. Bu, <code>start()</code> için bir kısayoldur—kolaylık için daha kısa.\n\n<div class=\"table-wrapper\"><table><thead><tr><th>Parametre</th><th>Tip</th><th>Gerekli</th><th>Açıklama</th></tr></thead><tbody><tr><td><code>callback</code></td><td><code>Function</code></td><td>Hayır</td><td>Sunucu başarıyla dinlemeye başladığında bir kez çağrılır. Argüman almaz.</td></tr></tbody></table></div>\n\n<h3(Örnekler)\n\n![code](const { Server } = require('oblit');\n\nconst app = new Server({ port: 8080 });\n\n// Callback ile başlat\napp.st(() => {\n    console.log('✅ Sunucu 8080 portunda başlatıldı');\n    console.log('Bağlantıları kabul etmeye hazır!');\n});\n\n// Veya callback olmadan başlat\napp.st();)!++//\n\n![tip](Callback, oyun durumu yükleme, veritabanlarına bağlanma veya \"sunucu hazır\" bildirimleri gönderme gibi başlatma görevleri için kullanışlıdır.)!++\n\nh3(<code>server.bc(message)</code>)\n\nŞu anda bağlı **tüm** istemcilere bir mesaj yayınlar. Bu, <code>broadcast()</code> için bir kısayoldur.\n\n<div class=\"table-wrapper\"><table><thead><tr><th>Parametre</th><th>Tip</th><th>Gerekli</th><th>Açıklama</th></tr></thead><tbody><tr><td><code>message</code></td><td><code>String | Object | Buffer</code></td><td>Evet</td><td>Yayınlanacak veri. Tip otomatik olarak tespit edilir (TXT/JSON/RAW).</td></tr></tbody></table></div>\n\n<h3(Performans Detayları)\n\n<div style=\"background:var(--box-tip-bg); padding:20px; border-radius:8px; margin:20px 0; border-left:4px solid #22c55e;\">\n<strong style=\"color:#22c55e;\">⚡ Optimizasyon:</strong><br><br>\nOBLIT veriyi **bir kez** paketler ve aynı buffer referansını tüm bağlı soketlere yazar. Bu şu anlama gelir:\n<ul style=\"margin:10px 0 0 0;\">\n<li>✅ İstemci başına sıfır bellek ek yükü</li>\n<li>✅ Minimal CPU kullanımı</li>\n<li>✅ Mümkün olan en hızlı yayınlama</li>\n</ul>\n</div>\n\n<h3(Örnekler)\n\n![code](const app = new Server({ port: 8080 });\n\napp.on('data', (message, socket) => {\n    // Metin mesajı yayınla\n    app.bc(`Kullanıcı dedi ki: ${message}`);\n    \n    // JSON nesnesi yayınla\n    app.bc({ type: 'chat', user: socket.remoteAddress, text: message });\n    \n    // Binary veri yayınla\n    const gameState = Buffer.from([1, 2, 3, 4]);\n    app.bc(gameState);\n});\n\napp.st();)!++//\n\n![info](Yayınlama tek yönlüdür. İstemciler kendi yayınlarına yanıt almaz. Çift yönlü iletişime ihtiyacınız varsa, <code>'data'</code> olayındaki <code>socket</code> parametresini kullanın.)!++\n\n!(line)\n\nh2(📡 Olaylar)\n\nServer sınıfı EventEmitter'ı genişletir, bu yüzden tüm standart EventEmitter metodlarını (<code>on</code>, <code>once</code>, <code>off</code>, vb.) kullanabilirsiniz.\n\nh3(<code>'data'</code> Olayı)\n\nBir istemciden tam bir paket alındığında yayınlanır. Bu, gelen mesajları işlemek için birincil olaydır.\n\n<div class=\"table-wrapper\"><table><thead><tr><th>Olay Adı</th><th>Callback Argümanları</th><th>Ne Zaman Yayınlanır</th></tr></thead><tbody><tr><td><code>'data'</code></td><td><code>(message, socket)</code></td><td>Herhangi bir istemciden tam bir paket geldiğinde</td></tr></tbody></table></div>\n\n<h3(Callback Parametreleri)\n\n<div class=\"table-wrapper\"><table><thead><tr><th>Parametre</th><th>Tip</th><th>Açıklama</th></tr></thead><tbody><tr><td><code>message</code></td><td><code>String | Object | Buffer</code></td><td>Paketi açılmış yük. Tip, istemci tarafından gönderilen OpCode'a bağlıdır.</td></tr><tr><td><code>socket</code></td><td><code>net.Socket</code></td><td>Mesajı gönderen istemcinin Node.js soket nesnesi. Bunu yanıt göndermek veya istemcileri tanımlamak için kullanın.</td></tr></tbody></table></div>\n\n<h3(Örnekler)\n\n![code](const { Server, OP } = require('oblit');\nconst core = require('oblit/src/c');\n\nconst app = new Server({ port: 8080 });\n\n// Metin mesajlarını işle\napp.on('data', (message, socket) => {\n    console.log(`${socket.remoteAddress} adresinden alındı:`, message);\n    \n    // Yanıtı sadece gönderene gönder\n    if (message === 'PING') {\n        const pongPacket = core.p(OP.TXT, 'PONG');\n        socket.write(pongPacket);\n    }\n    \n    // Herkese yayınla\n    if (typeof message === 'string' && message.startsWith('!')) {\n        app.bc(`Duyuru: ${message.substring(1)}`);\n    }\n});\n\n// JSON nesnelerini işle\napp.on('data', (message, socket) => {\n    if (typeof message === 'object' && !Buffer.isBuffer(message)) {\n        if (message.type === 'login') {\n            console.log(`${message.username} kullanıcısı giriş yaptı`);\n            socket.playerId = message.username;\n        }\n    }\n});\n\n// Binary veriyi işle\napp.on('data', (message, socket) => {\n    if (Buffer.isBuffer(message)) {\n        // Oyun koordinatlarını, sensör verilerini vb. işle\n        const playerId = message.readUInt16BE(0);\n        const x = message.readUInt16BE(2);\n        const y = message.readUInt16BE(4);\n        console.log(`Oyuncu ${playerId} (${x}, ${y}) konumuna taşındı`);\n    }\n});\n\napp.st();)!++//\n\nh3(<code>'connect'</code> Olayı)\n\nYeni bir istemci sunucuya başarıyla bağlandığında yayınlanır.\n\n<div class=\"table-wrapper\"><table><thead><tr><th>Olay Adı</th><th>Callback Argümanları</th><th>Ne Zaman Yayınlanır</th></tr></thead><tbody><tr><td><code>'connect'</code></td><td><code>(socket)</code></td><td>Yeni bir istemci TCP bağlantısı kurduktan hemen sonra</td></tr></tbody></table></div>\n\n<h3(Örnekler)\n\n![code](const app = new Server({ port: 8080 });\n\napp.on('connect', (socket) => {\n    const clientId = `${socket.remoteAddress}:${socket.remotePort}`;\n    console.log(`✅ Yeni bağlantı: ${clientId}`);\n    \n    // Sokette özel veri sakla\n    socket.connectedAt = Date.now();\n    socket.playerId = null;\n    \n    // Hoş geldin mesajı gönder\n    const welcome = { type: 'welcome', message: 'Sunucuya bağlandınız!' };\n    app.bc(welcome);\n});\n\napp.st();)!++//\n\nh3(<code>'disconnect'</code> Olayı)\n\nBir istemci bağlantısını kestiğinde (zarif veya beklenmedik şekilde) yayınlanır.\n\n<div class=\"table-wrapper\"><table><thead><tr><th>Olay Adı</th><th>Callback Argümanları</th><th>Ne Zaman Yayınlanır</th></tr></thead><tbody><tr><td><code>'disconnect'</code></td><td><code>(socket)</code></td><td>Bir istemcinin TCP bağlantısı kapandığında</td></tr></tbody></table></div>\n\n<h3(Örnekler)\n\n![code](const app = new Server({ port: 8080 });\n\napp.on('disconnect', (socket) => {\n    const clientId = socket.playerId || `${socket.remoteAddress}:${socket.remotePort}`;\n    console.log(`❌ İstemci bağlantısı kesildi: ${clientId}`);\n    \n    // Temizlik\n    if (socket.playerId) {\n        delete gameState.players[socket.playerId];\n    }\n    \n    // Diğer istemcilere bildir\n    app.bc({ type: 'player_left', playerId: socket.playerId });\n});\n\napp.st();)!++//\n\nh3(<code>'error'</code> Olayı)\n\nSunucu seviyesinde bir hata oluştuğunda yayınlanır (örn., port zaten kullanımda).\n\n<div class=\"table-wrapper\"><table><thead><tr><th>Olay Adı</th><th>Callback Argümanları</th><th>Ne Zaman Yayınlanır</th></tr></thead><tbody><tr><td><code>'error'</code></td><td><code>(error)</code></td><td>Sunucu seviyesinde bir hata oluştuğunda</td></tr></tbody></table></div>\n\n<h3(Örnekler)\n\n![code](const app = new Server({ port: 8080 });\n\napp.on('error', (error) => {\n    console.error('❌ Sunucu hatası:', error.message);\n    \n    if (error.code === 'EADDRINUSE') {\n        console.log('8080 portu zaten kullanımda. 8081 portu deneniyor...');\n        app.st(); // Bu çalışmayacak, ama deseni gösterir\n    }\n});\n\napp.st();)!++//\n\n!(line)\n\nh2(💡 En İyi Uygulamalar)\n\n<div style=\"display:grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap:15px; margin:25px 0;\">\n\n<div style=\"background:var(--box-tip-bg); padding:20px; border-radius:8px; border-left:4px solid #22c55e;\">\n<strong style=\"color:#22c55e;\">✅ İstemci Verilerini Sakla</strong><br><br>\nİstemci durumunu takip etmek için <code>socket</code> nesnesine özel özellikler ekleyin:\n<code style=\"background:transparent;\">socket.playerId = 'user123';</code>\n</div>\n\n<div style=\"background:var(--box-warn-bg); padding:20px; border-radius:8px; border-left:4px solid #f97316;\">\n<strong style=\"color:#f97316;\">⚠️ Hataları İşle</strong><br><br>\nSunucu seviyesindeki sorunları zarifçe işlemek için her zaman <code>'error'</code> olayını dinleyin.\n</div>\n\n<div style=\"background:var(--box-info-bg); padding:20px; border-radius:8px; border-left:4px solid var(--primary);\">\n<strong style=\"color:var(--primary);\">📦 Oyunlar İçin Binary Kullan</strong><br><br>\nYüksek frekanslı veriler için (oyuncu konumları gibi) maksimum performans için Buffer/RAW modunu kullanın.\n</div>\n\n</div>\n\n![tip](Tam çalışan bir örnek görmek ister misiniz? Gerçek dünya uygulamaları için **[Chat Eğitimi](#!/tut_chat)** veya **[Oyun Sunucusu Eğitimi](#!/tut_game)** sayfalarına bakın.)!++"
}
