{
    "en": "h1(Client API: Complete Reference)\nh3(Connect to OBLIT servers and send/receive data with ease.)\n\nThe <code>Client</code> class provides a lightweight, event-driven TCP client for connecting to OBLIT servers. It handles all the low-level protocol details, including stream reassembly and automatic type detection.\n\n![info](The Client API is intentionally simple. Just connect, send, and receive. OBLIT handles the rest automatically.)!++\n\n!(line)\n\nh2(📦 Constructor)\n\nh3(<code>new Client(address)</code>)\n\nCreates a new client instance. The client does **not** connect until you call <code>cn()</code>.\n\n<div class=\"table-wrapper\"><table><thead><tr><th>Parameter</th><th>Type</th><th>Required</th><th>Description</th></tr></thead><tbody><tr><td><code>address</code></td><td><code>String</code></td><td>Yes</td><td>Server address in format <code>'HOST:PORT'</code> (e.g., <code>'127.0.0.1:8080'</code>)</td></tr></tbody></table></div>\n\n<h3(Examples)\n\n![code](const { Client } = require('oblit');\n\n// Connect to localhost\nconst client = new Client('127.0.0.1:3000');\n\n// Connect to remote server\nconst remoteClient = new Client('example.com:8080');\n\n// Connect with default port (3000)\nconst defaultClient = new Client('192.168.1.100:3000');)!++//\n\n<div style=\"background:var(--box-info-bg); padding:15px; border-radius:8px; margin:15px 0; border-left:4px solid var(--primary);\">\n<strong>Address Format:</strong><br><br>\n• <code>HOST</code> can be an IP address (<code>192.168.1.1</code>) or domain name (<code>example.com</code>)<br>\n• <code>PORT</code> must be a number between 1 and 65535<br>\n• Format is always <code>'HOST:PORT'</code> with a colon separator\n</div>\n\n!(line)\n\nh2(⚙️ Methods)\n\nh3(<code>client.cn([callback])</code>)\n\nEstablishes a TCP connection to the server. This is an alias for <code>connect()</code>.\n\n<div class=\"table-wrapper\"><table><thead><tr><th>Parameter</th><th>Type</th><th>Required</th><th>Description</th></tr></thead><tbody><tr><td><code>callback</code></td><td><code>Function</code></td><td>No</td><td>Called once when connection is successfully established. Receives no arguments.</td></tr></tbody></table></div>\n\n<h3(Examples)\n\n![code](const client = new Client('127.0.0.1:8080');\n\n// Connect with callback\nclient.cn(() => {\n    console.log('✅ Connected to server!');\n    // Send initial message\n    client.snd('Hello from client!');\n});\n\n// Connect without callback\nclient.cn();\n\n// Or use the 'ready' event\nclient.on('ready', () => {\n    console.log('Server connection established');\n});\nclient.cn();)!++//\n\nh3(<code>client.snd(message)</code>)\n\nSends a message to the server. The data type is automatically detected and packed appropriately.\n\n<div class=\"table-wrapper\"><table><thead><tr><th>Parameter</th><th>Type</th><th>Required</th><th>Description</th></tr></thead><tbody><tr><td><code>message</code></td><td><code>String | Object | Buffer</code></td><td>Yes</td><td>The data to send. Type is auto-detected: String→OP.TXT, Object→OP.JSON, Buffer→OP.RAW</td></tr></tbody></table></div>\n\n<h3(Examples)\n\n![code](const client = new Client('127.0.0.1:8080');\nclient.cn(() => {\n    // Send text message\n    client.snd('Hello Server!');\n    \n    // Send JSON object\n    client.snd({ type: 'login', username: 'player1', score: 100 });\n    \n    // Send binary data (for games, sensor data, etc.)\n    const positionData = Buffer.alloc(6);\n    positionData.writeUInt16BE(42, 0);  // Player ID\n    positionData.writeUInt16BE(100, 2); // X coordinate\n    positionData.writeUInt16BE(200, 4); // Y coordinate\n    client.snd(positionData);\n    \n    // Send periodically\n    setInterval(() => {\n        client.snd({ type: 'heartbeat', timestamp: Date.now() });\n    }, 5000);\n});)!++//\n\n<div style=\"background:var(--box-tip-bg); padding:15px; border-radius:8px; margin:15px 0; border-left:4px solid #22c55e;\">\n<strong>💡 Tip:</strong> For high-frequency data (like player positions in games), use Buffer/RAW mode for maximum performance. See the **[Game Tutorial](#!/tut_game)** for examples.\n</div>\n\n!(line)\n\nh2(📡 Events)\n\nh3(<code>'ready'</code> Event)\n\nEmitted when the connection to the server is successfully established.\n\n![code](const client = new Client('127.0.0.1:8080');\n\nclient.on('ready', () => {\n    console.log('Connected and ready to send data!');\n    client.snd('First message');\n});\n\nclient.cn();)!++//\n\nh3(<code>'data'</code> Event)\n\nEmitted whenever a complete packet is received from the server.\n\n<div class=\"table-wrapper\"><table><thead><tr><th>Event Name</th><th>Callback Arguments</th><th>When Emitted</th></tr></thead><tbody><tr><td><code>'data'</code></td><td><code>(message)</code></td><td>When a complete packet arrives from the server</td></tr></tbody></table></div>\n\n<h3(Examples)\n\n![code](const client = new Client('127.0.0.1:8080');\n\nclient.on('data', (message) => {\n    // Handle different message types\n    if (typeof message === 'string') {\n        console.log('Text message:', message);\n    } else if (typeof message === 'object' && !Buffer.isBuffer(message)) {\n        console.log('JSON message:', message);\n        if (message.type === 'chat') {\n            console.log(`${message.user}: ${message.text}`);\n        }\n    } else if (Buffer.isBuffer(message)) {\n        console.log('Binary data received:', message);\n        // Unpack binary data\n        const playerId = message.readUInt16BE(0);\n        const x = message.readUInt16BE(2);\n        // ... process binary data\n    }\n});\n\nclient.cn();)!++//\n\nh3(<code>'close'</code> Event)\n\nEmitted when the connection to the server is closed.\n\n![code](const client = new Client('127.0.0.1:8080');\n\nclient.on('close', () => {\n    console.log('Connection closed. Attempting reconnect...');\n    // Implement reconnection logic here\n    setTimeout(() => client.cn(), 1000);\n});\n\nclient.cn();)!++//\n\nh3(<code>'error'</code> Event)\n\nEmitted when a connection error occurs.\n\n![code](const client = new Client('127.0.0.1:8080');\n\nclient.on('error', (error) => {\n    console.error('Connection error:', error.message);\n    \n    if (error.code === 'ECONNREFUSED') {\n        console.log('Server is not running or unreachable');\n    } else if (error.code === 'ETIMEDOUT') {\n        console.log('Connection timeout');\n    }\n});\n\nclient.cn();)!++//\n\n!(line)\n\nh2(💡 Complete Example)\n\nHere's a complete, production-ready client example:\n\n![code](const { Client } = require('oblit');\n\nconst client = new Client('127.0.0.1:8080');\n\n// Handle connection ready\nclient.on('ready', () => {\n    console.log('✅ Connected to server');\n    \n    // Send login message\n    client.snd({ type: 'login', username: 'player1' });\n});\n\n// Handle incoming messages\nclient.on('data', (message) => {\n    if (typeof message === 'object') {\n        switch (message.type) {\n            case 'welcome':\n                console.log('Server says:', message.text);\n                break;\n            case 'chat':\n                console.log(`[${message.user}] ${message.text}`);\n                break;\n            case 'game_update':\n                // Process game state update\n                updateGameState(message.data);\n                break;\n        }\n    }\n});\n\n// Handle connection close\nclient.on('close', () => {\n    console.log('❌ Connection lost. Reconnecting...');\n    setTimeout(() => client.cn(), 2000);\n});\n\n// Handle errors\nclient.on('error', (error) => {\n    console.error('Connection error:', error.message);\n});\n\n// Connect\nclient.cn();)!++//\n\n![tip](Want to see more examples? Check out the **[Chat Tutorial](#!/tut_chat)** and **[Game Server Tutorial](#!/tut_game)** for complete, working applications.)!++",
    "tr": "h1(İstemci API: Tam Referans)\nh3(OBLIT sunucularına bağlanın ve veri gönderin/alın.)\n\n<code>Client</code> sınıfı, OBLIT sunucularına bağlanmak için hafif, olay odaklı bir TCP istemcisi sağlar. Akış yeniden birleştirme ve otomatik tip tespiti dahil tüm düşük seviye protokol detaylarını işler.\n\n![info](İstemci API kasıtlı olarak basittir. Sadece bağlan, gönder ve al. OBLIT gerisini otomatik olarak halleder.)!++\n\n!(line)\n\nh2(📦 Constructor)\n\nh3(<code>new Client(address)</code>)\n\nYeni bir istemci örneği oluşturur. İstemci <code>cn()</code> çağrılana kadar **bağlanmaz**.\n\n<div class=\"table-wrapper\"><table><thead><tr><th>Parametre</th><th>Tip</th><th>Gerekli</th><th>Açıklama</th></tr></thead><tbody><tr><td><code>address</code></td><td><code>String</code></td><td>Evet</td><td><code>'HOST:PORT'</code> formatında sunucu adresi (örn., <code>'127.0.0.1:8080'</code>)</td></tr></tbody></table></div>\n\n<h3(Örnekler)\n\n![code](const { Client } = require('oblit');\n\n// localhost'a bağlan\nconst client = new Client('127.0.0.1:3000');\n\n// Uzak sunucuya bağlan\nconst remoteClient = new Client('example.com:8080');\n\n// Varsayılan port ile bağlan (3000)\nconst defaultClient = new Client('192.168.1.100:3000');)!++//\n\n<div style=\"background:var(--box-info-bg); padding:15px; border-radius:8px; margin:15px 0; border-left:4px solid var(--primary);\">\n<strong>Adres Formatı:</strong><br><br>\n• <code>HOST</code> bir IP adresi (<code>192.168.1.1</code>) veya domain adı (<code>example.com</code>) olabilir<br>\n• <code>PORT</code> 1 ile 65535 arasında bir sayı olmalıdır<br>\n• Format her zaman iki nokta ayırıcı ile <code>'HOST:PORT'</code> şeklindedir\n</div>\n\n!(line)\n\nh2(⚙️ Metodlar)\n\nh3(<code>client.cn([callback])</code>)\n\nSunucuya bir TCP bağlantısı kurar. Bu, <code>connect()</code> için bir kısayoldur.\n\n<div class=\"table-wrapper\"><table><thead><tr><th>Parametre</th><th>Tip</th><th>Gerekli</th><th>Açıklama</th></tr></thead><tbody><tr><td><code>callback</code></td><td><code>Function</code></td><td>Hayır</td><td>Bağlantı başarıyla kurulduğunda bir kez çağrılır. Argüman almaz.</td></tr></tbody></table></div>\n\n<h3(Örnekler)\n\n![code](const client = new Client('127.0.0.1:8080');\n\n// Callback ile bağlan\nclient.cn(() => {\n    console.log('✅ Sunucuya bağlandı!');\n    // İlk mesajı gönder\n    client.snd('İstemciden merhaba!');\n});\n\n// Callback olmadan bağlan\nclient.cn();\n\n// Veya 'ready' olayını kullan\nclient.on('ready', () => {\n    console.log('Sunucu bağlantısı kuruldu');\n});\nclient.cn();)!++//\n\nh3(<code>client.snd(message)</code>)\n\nSunucuya bir mesaj gönderir. Veri tipi otomatik olarak tespit edilir ve uygun şekilde paketlenir.\n\n<div class=\"table-wrapper\"><table><thead><tr><th>Parametre</th><th>Tip</th><th>Gerekli</th><th>Açıklama</th></tr></thead><tbody><tr><td><code>message</code></td><td><code>String | Object | Buffer</code></td><td>Evet</td><td>Gönderilecek veri. Tip otomatik tespit edilir: String→OP.TXT, Object→OP.JSON, Buffer→OP.RAW</td></tr></tbody></table></div>\n\n<h3(Örnekler)\n\n![code](const client = new Client('127.0.0.1:8080');\nclient.cn(() => {\n    // Metin mesajı gönder\n    client.snd('Merhaba Sunucu!');\n    \n    // JSON nesnesi gönder\n    client.snd({ type: 'login', username: 'oyuncu1', skor: 100 });\n    \n    // Binary veri gönder (oyunlar, sensör verileri vb. için)\n    const positionData = Buffer.alloc(6);\n    positionData.writeUInt16BE(42, 0);  // Oyuncu ID\n    positionData.writeUInt16BE(100, 2); // X koordinatı\n    positionData.writeUInt16BE(200, 4); // Y koordinatı\n    client.snd(positionData);\n    \n    // Periyodik olarak gönder\n    setInterval(() => {\n        client.snd({ type: 'heartbeat', timestamp: Date.now() });\n    }, 5000);\n});)!++//\n\n<div style=\"background:var(--box-tip-bg); padding:15px; border-radius:8px; margin:15px 0; border-left:4px solid #22c55e;\">\n<strong>💡 İpucu:</strong> Yüksek frekanslı veriler için (oyunlardaki oyuncu konumları gibi) maksimum performans için Buffer/RAW modunu kullanın. Örnekler için **[Oyun Eğitimi](#!/tut_game)** sayfasına bakın.\n</div>\n\n!(line)\n\nh2(📡 Olaylar)\n\nh3(<code>'ready'</code> Olayı)\n\nSunucuya bağlantı başarıyla kurulduğunda yayınlanır.\n\n![code](const client = new Client('127.0.0.1:8080');\n\nclient.on('ready', () => {\n    console.log('Bağlandı ve veri göndermeye hazır!');\n    client.snd('İlk mesaj');\n});\n\nclient.cn();)!++//\n\nh3(<code>'data'</code> Olayı)\n\nSunucudan tam bir paket alındığında yayınlanır.\n\n<div class=\"table-wrapper\"><table><thead><tr><th>Olay Adı</th><th>Callback Argümanları</th><th>Ne Zaman Yayınlanır</th></tr></thead><tbody><tr><td><code>'data'</code></td><td><code>(message)</code></td><td>Sunucudan tam bir paket geldiğinde</td></tr></tbody></table></div>\n\n<h3(Örnekler)\n\n![code](const client = new Client('127.0.0.1:8080');\n\nclient.on('data', (message) => {\n    // Farklı mesaj tiplerini işle\n    if (typeof message === 'string') {\n        console.log('Metin mesajı:', message);\n    } else if (typeof message === 'object' && !Buffer.isBuffer(message)) {\n        console.log('JSON mesajı:', message);\n        if (message.type === 'chat') {\n            console.log(`${message.user}: ${message.text}`);\n        }\n    } else if (Buffer.isBuffer(message)) {\n        console.log('Binary veri alındı:', message);\n        // Binary veriyi aç\n        const playerId = message.readUInt16BE(0);\n        const x = message.readUInt16BE(2);\n        // ... binary veriyi işle\n    }\n});\n\nclient.cn();)!++//\n\nh3(<code>'close'</code> Olayı)\n\nSunucuya bağlantı kapandığında yayınlanır.\n\n![code](const client = new Client('127.0.0.1:8080');\n\nclient.on('close', () => {\n    console.log('Bağlantı kapatıldı. Yeniden bağlanılıyor...');\n    // Yeniden bağlanma mantığını buraya uygula\n    setTimeout(() => client.cn(), 1000);\n});\n\nclient.cn();)!++//\n\nh3(<code>'error'</code> Olayı)\n\nBağlantı hatası oluştuğunda yayınlanır.\n\n![code](const client = new Client('127.0.0.1:8080');\n\nclient.on('error', (error) => {\n    console.error('Bağlantı hatası:', error.message);\n    \n    if (error.code === 'ECONNREFUSED') {\n        console.log('Sunucu çalışmıyor veya erişilemiyor');\n    } else if (error.code === 'ETIMEDOUT') {\n        console.log('Bağlantı zaman aşımı');\n    }\n});\n\nclient.cn();)!++//\n\n!(line)\n\nh2(💡 Tam Örnek)\n\nİşte tam, üretime hazır bir istemci örneği:\n\n![code](const { Client } = require('oblit');\n\nconst client = new Client('127.0.0.1:8080');\n\n// Bağlantı hazır olayını işle\nclient.on('ready', () => {\n    console.log('✅ Sunucuya bağlandı');\n    \n    // Giriş mesajı gönder\n    client.snd({ type: 'login', username: 'oyuncu1' });\n});\n\n// Gelen mesajları işle\nclient.on('data', (message) => {\n    if (typeof message === 'object') {\n        switch (message.type) {\n            case 'welcome':\n                console.log('Sunucu diyor ki:', message.text);\n                break;\n            case 'chat':\n                console.log(`[${message.user}] ${message.text}`);\n                break;\n            case 'game_update':\n                // Oyun durumu güncellemesini işle\n                updateGameState(message.data);\n                break;\n        }\n    }\n});\n\n// Bağlantı kapatma olayını işle\nclient.on('close', () => {\n    console.log('❌ Bağlantı kesildi. Yeniden bağlanılıyor...');\n    setTimeout(() => client.cn(), 2000);\n});\n\n// Hataları işle\nclient.on('error', (error) => {\n    console.error('Bağlantı hatası:', error.message);\n});\n\n// Bağlan\nclient.cn();)!++//\n\n![tip](Daha fazla örnek görmek ister misiniz? Tam, çalışan uygulamalar için **[Chat Eğitimi](#!/tut_chat)** ve **[Oyun Sunucusu Eğitimi](#!/tut_game)** sayfalarına bakın.)!++"
}
